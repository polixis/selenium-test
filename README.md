# Selenium test app with Gecko and Chrome drivers
run:

    java -jar {jarFile} {selenium drivers parent dir}

Make sure you have "chromedriver" and "geckodriver" executables in the drivers parent directory